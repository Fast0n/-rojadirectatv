<resources>
    <!-- Base application theme. -->
    <!-- Light/Day Mode Theme -->
    <style name="AppTheme" parent="Theme.MaterialComponents.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">#000</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="windowNoTitle">true</item>

        <item name="android:statusBarColor">#F1F1F1</item>
        <item name="android:windowLightStatusBar">true</item>
        <item name="color_background">#F1F1F1</item>
        <item name="color_text">#000</item>
        <item name="color_toolbar">#c00</item>
        <item name="color_object">#F5F5F5</item>
    </style>

    <!-- Night/Dark Mode Theme -->
    <style name="DarkTheme" parent="Theme.MaterialComponents.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">#212121</item>
        <item name="colorAccent">#3e4855</item>
        <item name="windowNoTitle">true</item>

        <item name="android:statusBarColor">#2B2B2B</item>
        <item name="android:windowLightStatusBar">false</item>
        <item name="color_background">#2B2B2B</item>
        <item name="color_text">#fff</item>
        <item name="color_toolbar">#141414</item>
        <item name="color_object">#2B2B2B</item>
    </style>

    <style name="AppTheme.MySwitch" parent="@style/Widget.AppCompat.CompoundButton.Switch">
        <item name="android:textColorPrimaryDisableOnly">#fafafa</item>
        <item name="android:colorControlActivated">#c00</item>
        <item name="android:textAppearance">@style/TextAppearance.AppCompat</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
    </style>


    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="AppTheme.NoActionBar.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.NoActionBar.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />


    <!-- set the rounded drawable as background to your bottom sheet -->
    <style name="BottomSheet" parent="@style/Widget.Design.BottomSheet.Modal">
        <item name="android:background">@drawable/bg_bottom_sheet_dialog_fragment</item>
    </style>

    <style name="BaseBottomSheetDialog" parent="@style/Theme.Design.Light.BottomSheetDialog">
        <item name="android:windowIsFloating">false</item>
        <item name="bottomSheetStyle">@style/BottomSheet</item>
    </style>

    <style name="BottomSheetDialogTheme" parent="BaseBottomSheetDialog" />


</resources>
